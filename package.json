{
  "name": "cesium-effect-bigemap",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite --force",
    "build": "vite build",
    "preview": "vite preview",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -w",
    "changelog-r": "conventional-changelog -p angular -i CHANGELOG.md -w -r 0"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "cesium": "^1.92.0",
    "cesium-navigation-es6": "^3.0.8",
    "kriging-contour": "^1.0.5"
  }
}